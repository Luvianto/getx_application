import 'package:flutter/material.dart';

class UniversityCreatePage extends StatelessWidget {
  const UniversityCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('University Create Page'),
      ),
    );
  }
}
